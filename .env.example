# VFF Group — environment configuration
# Copy this file to .env and fill in values. Never commit .env to git.

# Django
DEBUG=True
SECRET_KEY=change-me-in-production

# Firebase Cloud Messaging (FCM HTTP v1)
FIREBASE_PROJECT_ID=vff-group-b185c
FIREBASE_CLIENT_EMAIL=android-app-latest@vff-group-b185c.iam.gserviceaccount.com
FIREBASE_SERVICE_ACCOUNT_FILE=credentials/firebase-service-account.json

# Optional: legacy fallback (deprecated — remove once v1 is verified)
# FCM_LEGACY_SERVER_KEY=
